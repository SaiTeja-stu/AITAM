package com.cybershield.domain;

/** The kinds of digital content the platform can analyze (problem statement scope). */
public enum ContentType {
    URL,
    EMAIL,
    SMS,
    QR,
    WEBPAGE,
    SOCIAL
}
